<UserControl x:Class="AI.Views.Admin.DeveloperView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="32,24,32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Developer" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,20"/>

        <!-- Configuration -->
        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left column: provider/endpoint/key/model -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Provider" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <ComboBox x:Name="ProviderCombo" Margin="0,0,0,12"
                              SelectionChanged="ProviderCombo_SelectionChanged"/>

                    <TextBlock Text="Endpoint URL" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox x:Name="EndpointBox" Margin="0,0,0,12"/>

                    <TextBlock Text="API Key" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox x:Name="ApiKeyBox" Margin="0,0,0,12"/>

                    <TextBlock Text="Model" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="ModelBox" Grid.Column="0"/>
                        <ComboBox x:Name="ModelCombo" Grid.Column="2"
                                  SelectionChanged="ModelCombo_SelectionChanged"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Temperature" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                            <TextBox x:Name="TemperatureBox" PlaceholderText="0.7"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Max Tokens" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                            <TextBox x:Name="MaxTokensBox" PlaceholderText="4096"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <!-- Right column: system prompt + prompt -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="System Prompt" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox x:Name="SystemPromptBox" AcceptsReturn="True" TextWrapping="Wrap"
                             Height="80" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12"/>

                    <TextBlock Text="Prompt" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox x:Name="PromptBox" AcceptsReturn="True" TextWrapping="Wrap"
                             Height="140" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12"/>

                    <Button x:Name="SendBtn" Content="Send Request" HorizontalAlignment="Left"
                            Click="SendBtn_Click"/>
                    <TextBlock x:Name="SendStatus" Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Result -->
        <Border Grid.Row="2" Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="RESPONSE" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="TokensLabel" Foreground="{StaticResource TextSecondaryBrush}"
                               FontSize="11" Margin="16,0,0,0" VerticalAlignment="Center"/>
                    <Button Content="Copy" Style="{StaticResource SecondaryButton}"
                            Margin="16,0,0,0" Padding="8,2" FontSize="11"
                            Click="CopyResultBtn_Click"/>
                </StackPanel>
                <TextBox Grid.Row="1" x:Name="ResultBox" IsReadOnly="True"
                         AcceptsReturn="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="12"
                         Background="Transparent" BorderThickness="0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
