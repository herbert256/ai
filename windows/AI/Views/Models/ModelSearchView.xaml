<UserControl x:Class="AI.Views.Models.ModelSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="32,24,32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Model Search" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,20"/>

        <!-- Controls -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="ProviderCombo" Grid.Column="0" Margin="0,0,8,0"
                      SelectionChanged="ProviderCombo_SelectionChanged"/>
            <TextBox x:Name="SearchBox" Grid.Column="1" Margin="0,0,8,0"
                     PlaceholderText="Search models..." TextChanged="SearchBox_TextChanged"/>
            <Button x:Name="FetchBtn" Grid.Column="2" Content="Fetch Models"
                    Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="FetchBtn_Click"/>
            <Button x:Name="TestBtn" Grid.Column="3" Content="Test Model" Click="TestBtn_Click"/>
        </Grid>

        <!-- Results -->
        <Border Grid.Row="2" Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="6">
            <Grid>
                <ListBox x:Name="ModelList" SelectionChanged="ModelList_SelectionChanged"
                         Background="Transparent" BorderThickness="0"/>
                <TextBlock x:Name="EmptyText" Text="Select a provider and fetch models"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Status / test result -->
        <Border Grid.Row="3" Margin="0,12,0,0" Padding="12" CornerRadius="6"
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1">
            <TextBlock x:Name="StatusText" Text="Select a model and click Test Model"
                       Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
        </Border>
    </Grid>
</UserControl>
