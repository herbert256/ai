<UserControl x:Class="AI.Views.Reports.ContentDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource CardBackgroundBrush}"
            BorderBrush="{StaticResource CardBorderBrush}"
            BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,12">
        <StackPanel>
            <!-- Header -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="AgentNameText" Grid.Column="0" FontSize="15" FontWeight="SemiBold"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock x:Name="TokenText" Foreground="{StaticResource TextSecondaryBrush}"
                               FontSize="11" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock x:Name="CostText" Foreground="{StaticResource SuccessBrush}"
                               FontSize="11" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Error or analysis -->
            <TextBlock x:Name="ErrorText" Foreground="{StaticResource ErrorBrush}"
                       TextWrapping="Wrap" Visibility="Collapsed" Margin="0,0,0,8"/>
            <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="AnalysisText" TextWrapping="Wrap" FontSize="13"
                           Foreground="{StaticResource OnBackgroundBrush}" LineHeight="20"/>
            </ScrollViewer>

            <!-- Citations -->
            <ItemsControl x:Name="CitationsList" Margin="0,8,0,0" Visibility="Collapsed">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="{StaticResource PrimaryBrush}"
                                   FontSize="11" Margin="0,2,0,0" TextWrapping="Wrap"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>
