<UserControl x:Class="AI.Views.Reports.NewReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="32,24,32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="New Report" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,24"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="Title" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
            <TextBox x:Name="TitleBox" PlaceholderText="Report title (optional)"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="Prompt" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
        </StackPanel>

        <TextBox Grid.Row="3" x:Name="PromptBox"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 PlaceholderText="Enter your prompt here..."
                 VerticalContentAlignment="Top"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="ClearBtn" Content="Clear" Style="{StaticResource SecondaryButton}"
                    Margin="0,0,12,0" Click="ClearBtn_Click"/>
            <Button x:Name="GenerateBtn" Content="Generate Report" Click="GenerateBtn_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
