<UserControl x:Class="AI.Views.Reports.ReportProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="32,24,32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TitleText" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" x:Name="StatusText" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,16"/>

        <ProgressBar Grid.Row="2" x:Name="ProgressBar" Height="6" Margin="0,0,0,24"
                     Minimum="0" Maximum="1" Value="0"/>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ResultsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource CardBackgroundBrush}"
                                BorderBrush="{StaticResource CardBorderBrush}"
                                BorderThickness="1" CornerRadius="6" Margin="0,0,0,8" Padding="12">
                            <StackPanel>
                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding Analysis}" TextWrapping="Wrap" MaxHeight="80"
                                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="StopBtn" Content="Stop" Style="{StaticResource DangerButton}"
                    Margin="0,0,12,0" Click="StopBtn_Click"/>
            <Button x:Name="DismissBtn" Content="Close" Style="{StaticResource SecondaryButton}"
                    Click="DismissBtn_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
