<UserControl x:Class="AI.Views.Settings.AgentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24,16,24,16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- List -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Agents" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,12"/>
            <Button Grid.Row="1" Content="+ New Agent" Margin="0,0,0,8" Click="NewBtn_Click"/>
            <Border Grid.Row="2" Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="6">
                <ListBox x:Name="AgentList" SelectionChanged="AgentList_SelectionChanged"
                         Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,2,0,2">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding ProviderId}" FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Grid>

        <!-- Edit form -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="EditPanel" Margin="0,0,0,24">
                <TextBlock x:Name="FormTitle" Text="Select an agent" FontSize="18"
                           FontWeight="SemiBold" Margin="0,0,0,16"/>

                <TextBlock Text="Name" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                <TextBox x:Name="NameBox" Margin="0,0,0,12"/>

                <TextBlock Text="Provider" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                <ComboBox x:Name="ProviderCombo" Margin="0,0,0,12"
                          SelectionChanged="ProviderCombo_SelectionChanged"/>

                <TextBlock Text="Model" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                <ComboBox x:Name="ModelCombo" IsEditable="True" Margin="0,0,0,12"/>

                <TextBlock Text="API Key (override)" Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,0,0,4"/>
                <TextBox x:Name="ApiKeyBox" Margin="0,0,0,16"/>

                <StackPanel Orientation="Horizontal">
                    <Button Content="Save" Margin="0,0,8,0" Click="SaveBtn_Click"/>
                    <Button Content="Delete" Style="{StaticResource DangerButton}" Click="DeleteBtn_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
