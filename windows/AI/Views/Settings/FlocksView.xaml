<UserControl x:Class="AI.Views.Settings.FlocksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24,16,24,16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- List -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Flocks" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,12"/>
            <Button Grid.Row="1" Content="+ New Flock" Margin="0,0,0,8" Click="NewBtn_Click"/>
            <Border Grid.Row="2" Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="6">
                <ListBox x:Name="FlockList" SelectionChanged="FlockList_SelectionChanged"
                         Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,2,0,2">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}">
                                    <Run Text="{Binding AgentIds.Count}"/>
                                    <Run Text=" agents"/>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Grid>

        <!-- Edit form -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,0,24">
                <TextBlock x:Name="FormTitle" Text="Select a flock" FontSize="18"
                           FontWeight="SemiBold" Margin="0,0,0,16"/>

                <TextBlock Text="Name" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                <TextBox x:Name="NameBox" Margin="0,0,0,16"/>

                <TextBlock Text="Agents" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                <Border Background="{StaticResource CardBackgroundBrush}"
                        BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="4"
                        Height="180" Margin="0,0,0,16">
                    <ListBox x:Name="AgentCheckList" SelectionMode="Multiple"
                             Background="Transparent" BorderThickness="0"
                             SelectionChanged="AgentCheckList_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

                <StackPanel Orientation="Horizontal">
                    <Button Content="Save" Margin="0,0,8,0" Click="SaveBtn_Click"/>
                    <Button Content="Delete" Style="{StaticResource DangerButton}" Click="DeleteBtn_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
