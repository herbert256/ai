<UserControl x:Class="AI.Views.Settings.PromptsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="24,16,24,16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- List panel -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Prompts" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,12"/>

            <Button Grid.Row="1" Content="+ Add Prompt" Margin="0,0,0,8" Click="NewBtn_Click"/>

            <!-- Info card -->
            <Border Grid.Row="2" Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="4"
                    Padding="8" Margin="0,0,0,8">
                <TextBlock Text="Variables: @MODEL@, @PROVIDER@, @AGENT@, @SWARM@, @NOW@"
                           FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
            </Border>

            <Border Grid.Row="3" Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="6">
                <ListBox x:Name="PromptList" SelectionChanged="PromptList_SelectionChanged"
                         Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2,0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding AgentName}" FontSize="11"
                                               Foreground="{StaticResource PrimaryBrush}"/>
                                    <TextBlock Text="{Binding Preview}" FontSize="11"
                                               Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                                <Button Grid.Column="1" Content="X" VerticalAlignment="Top"
                                        Style="{StaticResource DangerButton}" Tag="{Binding Id}"
                                        Click="DeleteBtn_Click" Padding="4,2" FontSize="10"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <TextBlock Grid.Row="4" x:Name="CountLabel" Text="0 prompts" Margin="0,8,0,0"
                       FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
        </Grid>

        <!-- Edit form -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="EditPanel" Margin="0,0,0,24">
                <TextBlock x:Name="FormTitle" Text="Select a prompt" FontSize="18"
                           FontWeight="SemiBold" Margin="0,0,0,16"/>

                <TextBlock Text="Name" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                <TextBox x:Name="NameBox" Margin="0,0,0,12"/>

                <TextBlock Text="Agent" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                <ComboBox x:Name="AgentCombo" Margin="0,0,0,12"/>

                <TextBlock Text="Prompt Text" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                <TextBox x:Name="PromptTextBox" AcceptsReturn="True" TextWrapping="Wrap"
                         Height="200" VerticalScrollBarVisibility="Auto"
                         VerticalContentAlignment="Top" Margin="0,0,0,16"/>

                <StackPanel Orientation="Horizontal">
                    <Button x:Name="SaveBtn" Content="Save" Margin="0,0,8,0" Click="SaveBtn_Click"/>
                    <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="CancelBtn_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
