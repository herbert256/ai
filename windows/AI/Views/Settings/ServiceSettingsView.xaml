<UserControl x:Class="AI.Views.Settings.ServiceSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24,16,24,16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Provider list -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Service Settings" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12"/>
            <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="6">
                <ListBox x:Name="ServiceList" SelectionChanged="ServiceList_SelectionChanged"
                         DisplayMemberPath="DisplayName" Background="Transparent" BorderThickness="0"/>
            </Border>
        </Grid>

        <!-- Detail -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="DetailPanel"/>
        </ScrollViewer>
    </Grid>
</UserControl>
